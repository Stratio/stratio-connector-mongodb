#A This function should be modified with what the user wants to do previous to
# the compilation.
# For instance, untar the code.

## directory/to/be/created

#DEB

mkdir -p /var/sds/stratio-connector-mongo/stratio-connector-mongo-store
mkdir -p /var/log/sds/stratio-connector-mongo
#mkdir -p /opt/sds/stratio-connector-mongodb/bin/stratio-connector-mongodb

