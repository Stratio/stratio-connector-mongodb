# This function should be modified with what the user wants to do previous to
# the compilation.
# For instance, untar the code.

## directory/to/be/created
mkdir -p /var/sds/stratio-connector-mongodb/stratio-connector-mongodb-store
mkdir -p /var/log/sds/stratio-connector-mongodb
mkdir -p /opt/sds/stratio-connector-mongodb
